services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    restart: unless-stopped

  client:
    build: ./client
    ports:
      - "5173:5173"
    environment:
      - WS_TARGET=server
    depends_on:
      - server
    restart: unless-stopped
